import { Shell } from "./_components/Shell";

// Self-contained RBAC demo. The whole UI adapts to the role selected in the
// header switcher — Customer / Admin / Owner.
export default function RbacPage() {
  return <Shell />;
}
